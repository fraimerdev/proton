node_modules/
dist/
.output/
.turbo/
coverage/
*.tsbuildinfo

# Secrets. PLAN.md I10: the real bot token never appears in a committed file.
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# TanStack Start build scratch
.tanstack/

# Agent scratch. .claude/worktrees holds entire repo checkouts; .impeccable/live holds a running
# preview server's pid, port and token.
.claude/worktrees/
**/.impeccable/live/
