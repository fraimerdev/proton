{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Proton React (dashboard)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["bun"]
  }
}
